{
	"name": "flow-lenia-viewer",
	"version": "2.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"build:wasm": "cd .. && wasm-pack build --target web --release",
		"clean": "rm -rf dist",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"check": "biome check --write .",
		"typecheck": "svelte-check --tsconfig ./tsconfig.json",
		"ci": "biome ci . && svelte-check --tsconfig ./tsconfig.json"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"@sveltejs/vite-plugin-svelte": "^5.0.3",
		"@types/bun": "latest",
		"@webgpu/types": "^0.1.68",
		"svelte": "^5.16.0",
		"svelte-check": "^4.1.0",
		"typescript": "^5.3.0",
		"vite": "^6.0.7"
	},
	"dependencies": {
		"@types/three": "^0.182.0",
		"three": "^0.182.0"
	}
}
