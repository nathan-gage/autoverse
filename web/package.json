{
	"name": "flow-lenia-viewer",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"dev": "bun --bun run dev-server.ts",
		"build": "bun run build.ts",
		"build:wasm": "cd .. && wasm-pack build --target web --release",
		"preview": "bunx serve dist -l 3000",
		"clean": "rm -rf dist",
		"lint": "biome lint .",
		"format": "biome format --write .",
		"check": "biome check --write .",
		"ci": "biome ci ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.11",
		"@types/bun": "latest",
		"typescript": "^5.3.0"
	}
}
